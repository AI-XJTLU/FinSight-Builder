# Docker Runtime Check

Checked on 1 June 2026 after:

```powershell
docker compose -f Task1/artifacts/app/docker/docker-compose.yml up --build -d
```

Results:

- `GET /` returned HTTP 200 and served the dashboard.
- The homepage references `generated_market_banner.png`, shows the exact educational disclaimer, and includes the product-facing footer.
- `GET /health` returned HTTP 200 with `status: ok`.
- `GET /api/stocks` returned HTTP 200 with four fixed educational sample stocks.
- `GET /api/risk-summary` returned HTTP 200.
- `GET /stock/AAPL` returned HTTP 200 and served the stock detail page.
- `GET /static/generated_market_banner.png` returned HTTP 200 with the generated PNG asset.
- A valid `POST /api/watchlist` request returned HTTP 201.
- A valid `POST /api/feedback` request returned HTTP 201.

The Docker Compose container was running with port mapping `5005:5005`.
